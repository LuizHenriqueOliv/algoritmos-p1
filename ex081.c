#include <stdio.h>

void main()
{
    float circ_x, circ_y, raio;
    printf("Informe o valor das coordenadas (X e Y) que representam o centro da circunferencia, e o valor do seu raio: ");
    scanf("%f%f", &coord_x, coord_y, raio);


    printf("Agora informe o valor das coordenadas de um ponto P: ");
    scanf("%f%f", )
}